# fakestore-data-engineering
ETL pipeline (Bronze → Silver → Gold) using FakeStore API, Delta Lake (delta-rs), Pandas, PyArrow and Google Colab. Includes ingestion, transformation, and analytics layer.
